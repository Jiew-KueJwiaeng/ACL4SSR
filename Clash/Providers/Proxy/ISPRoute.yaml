payload:
  - AND,((GEOIP,CLOUDFLARE),(NETWORK,tcp))
  - AND,((GEOIP,cloudflare),(NETWORK,tcp))
  - AND,((GEOIP,CLOUDFRONT),(NETWORK,tcp))
  - AND,((GEOIP,cloudfront),(NETWORK,tcp))
  - AND,((GEOIP,FACEBOOK),(NETWORK,tcp))
  - AND,((GEOIP,facebook),(NETWORK,tcp))
  - AND,((GEOIP,FASTLY),(NETWORK,tcp))
  - AND,((GEOIP,fastly),(NETWORK,tcp))
  - AND,((GEOIP,GOOGLE),(NETWORK,tcp))
  - AND,((GEOIP,google),(NETWORK,tcp))
  - AND,((GEOIP,NETFLIX),(NETWORK,tcp))
  - AND,((GEOIP,netflix),(NETWORK,tcp))
  - AND,((GEOIP,TELEGRAM),(NETWORK,tcp))
  - AND,((GEOIP,telegram),(NETWORK,tcp))
  - AND,((GEOIP,TWITTER),(NETWORK,tcp))
  - AND,((GEOIP,twitter),(NETWORK,tcp))
