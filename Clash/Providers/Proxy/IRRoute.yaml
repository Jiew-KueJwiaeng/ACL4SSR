payload:
  - AND,((GEOSITE,category-gov-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-insurance-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-media-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-news-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-payment-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-scholar-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-shopping-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-social-media-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-tech-ir),(NETWORK,tcp))
  - AND,((GEOSITE,category-travel-ir),(NETWORK,tcp))
  - AND,((GEOIP,ir),(NETWORK,tcp))
  - AND,((GEOIP,IR),(NETWORK,tcp))
