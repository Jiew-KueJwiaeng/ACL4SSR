payload:
  - AND,((GEOIP,us),(NETWORK,tcp))
  - AND,((GEOIP,US),(NETWORK,tcp))
  - AND,((GEOIP,as),(NETWORK,tcp))
  - AND,((GEOIP,AS),(NETWORK,tcp))
  - AND,((GEOIP,gu),(NETWORK,tcp))
  - AND,((GEOIP,GU),(NETWORK,tcp))
  - AND,((GEOIP,mp),(NETWORK,tcp))
  - AND,((GEOIP,MP),(NETWORK,tcp))
  - AND,((GEOIP,pr),(NETWORK,tcp))
  - AND,((GEOIP,PR),(NETWORK,tcp))
  - AND,((GEOIP,um),(NETWORK,tcp))
  - AND,((GEOIP,UM),(NETWORK,tcp))
  - AND,((GEOIP,vi),(NETWORK,tcp))
  - AND,((GEOIP,VI),(NETWORK,tcp))
